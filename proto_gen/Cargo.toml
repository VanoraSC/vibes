[package]
name = "proto_gen"
version = "0.1.0"
edition = "2024"

[dependencies]
prost = "0.12"
tonic = { version = "0.11", features = ["transport"] }

[build-dependencies]
prost-build = "0.12"
tonic-build = "0.11"
